# Feature PRD: F1.3 — Token Validation & Game Access Gate

## 1. Feature Name

**Token Validation & Game Access Gate** — Validates the received token with the backend and controls whether the player may access the game, displaying appropriate error screens for every failure mode.

## 2. Epic

- **Parent Epic:** [E1 — Auth Bridge](./_epic-E1-auth-bridge.md)
- **Priority:** P0 — Critical

## 3. Purpose & Scope

Once a token is received (from F1.1, F1.2, or localStorage), the WebView must confirm its validity with the backend before the player can access any game screen. This feature is the enforcement point of the auth gate: no player reaches the game without a validated identity. It also owns all error and recovery UX for every auth failure mode, and is responsible for persisting a validated token to localStorage so other WebView pages can load without requiring the mobile app to re-inject the token.

**Intended Audience:** Engineering team.

**Assumptions:**
- Token validation is performed by submitting the token to the project backend (not to the Coupon API directly).
- The backend response determines whether the token is valid, expired, or unauthorized.
- The frontend cannot decode or inspect the token's claims itself — validation is server-authoritative.
- Token resolution follows a strict priority order: URL param (`?accessToken=`) → retry path → localStorage → JS bridge.
- If both URL param and JS bridge provide a token, the URL param always wins (first-in-priority, not first-in-time).
- The player has no way to "log in again" from within the WebView — the only recovery path for auth failures is to re-open the WebView from the native app.
- All error screen strings must exist in both `ja` and `en` locale files (`auth.json`).
- When `VITE_ENV=local`, the app is running in a local development environment without a mobile app host. In this mode, token collection (URL param, JS bridge, localStorage) is skipped and a synthetic `local-dev-token` is submitted for validation. The backend (`ENV=local`) must accept this synthetic token and return a valid response. The auth gate still runs — only the token source differs.

## 4. User Personas

| Persona | Description |
|---|---|
| **PLAYER** | Expects to arrive at the game immediately; if auth fails, needs a clear explanation of what happened and what to do next |
| **HOST** | Responsible for providing a fresh, valid token; may re-open the WebView with a new token if the player requests it |
| **SYSTEM** | The project backend; authoritative source for token validity; returns structured error codes the frontend acts upon |

## 5. User Stories

- As a **PLAYER**, I want the game to be available immediately after I open it so that I don't have to wait or log in.
- As a **PLAYER**, I want a clear message when something goes wrong with my session so that I know whether to try again or go back to the app.
- As a **HOST**, I want the WebView to reject invalid or expired tokens so that unauthorized players cannot access the game.
- As a **SYSTEM**, I want to be the sole authority on token validity so that the frontend cannot be tricked by a manipulated token.

## 6. Requirements

### Functional Requirements

- **REQ-301**: The system shall display a loading screen immediately upon receiving a token from any channel (F1.1, F1.2, or localStorage), before the validation response is received.
- **REQ-302**: The system shall submit the received token to the backend validation endpoint and await the response before rendering any game screen.
- **REQ-303**: When the backend confirms the token is valid, the system shall grant the player access to the game, navigate to the game entry screen, and persist the token to localStorage (`access_token` key).
- **REQ-304**: When no token is available from URL param, localStorage, or JS bridge within the designated wait period, the system shall display the `auth.tokenMissing` error screen.
- **REQ-305**: When the backend returns a token-expired error, the system shall display the `auth.tokenExpired` error screen.
- **REQ-306**: When the backend returns a token-invalid or signature-failure error, the system shall display the `auth.tokenInvalid` error screen.
- **REQ-307**: When the backend returns a 401 Unauthorized response, the system shall display the `auth.unauthorized` error screen.
- **REQ-308**: When the backend returns a 500 or 503 response, the system shall display the `auth.serverError` error screen with a manual retry option.
- **REQ-309**: The validated token shall be held in `AuthContext` for the duration of the session and persisted to localStorage so that navigating to other WebView pages does not require the mobile app to re-inject the token.
- **REQ-310**: On page load when no URL param is present and no retry is pending, the system shall check localStorage for a previously validated token and submit it for re-validation before registering the JS bridge listener.
- **REQ-311**: When validation fails for a token sourced from localStorage (expired or invalid response), the system shall remove the token from localStorage before displaying the error screen.
- **REQ-312**: When `VITE_ENV=local`, the system shall skip all token collection channels (URL param, JS bridge, localStorage) and immediately submit the synthetic token `local-dev-token` to the backend validation endpoint. The backend (`ENV=local`) shall accept this token and return a successful validation response. The auth gate flow continues normally from validation onward — no error screen is shown, the token is saved to `AuthContext`, and the game loads.
- **REQ-313**: When `VITE_ENV=local`, the validated `local-dev-token` shall **not** be written to localStorage. This prevents the synthetic token from persisting across environment switches and causing spurious auth errors when `VITE_ENV` is later changed to `staging` or `production`.

### Security Requirements

- **SEC-301**: The system shall never grant game access based on client-side inspection of the token (e.g., decoding the JWT payload). Validation is server-authoritative.
- **SEC-302**: The token shall be written only to localStorage (`access_token` key) after a successful backend validation response — never before, never to sessionStorage or cookies.
- **SEC-303**: The token shall be removed from localStorage when it fails backend validation (expired or invalid). It is not explicitly cleared on WebView close; the next open will re-validate via localStorage or require a fresh injection.
- **SEC-304**: Error screens must not expose the raw token value or backend error internals to the player.

### Validation Rules

- **VAL-301**: A token that is structurally empty or null shall not be submitted to the backend; the system shall treat it as a missing token (REQ-304).
- **VAL-302**: The system shall submit only the first valid non-empty token received; subsequent tokens from the other channel shall not trigger a second validation unless the first has definitively failed and the player triggers a retry.

### Constraints

- **CON-301**: There is no in-WebView re-authentication flow. Every error screen (except server error with retry) instructs the player to close the WebView and re-open it from the native app.
- **CON-302**: The game access gate is absolute — no game content is rendered without a successful backend validation response. `VITE_ENV=local` does not bypass the gate; it provides a synthetic token that the backend accepts in its own local mode (`ENV=local`). The validation code path is identical in all environments.

### Guidelines

- **GUD-301**: The loading screen should be visually consistent with the game's warm Japanese aesthetic to avoid a jarring transition.
- **GUD-302**: Error screens should be friendly and non-technical — "お時間が経過しました。アプリからもう一度お試しください。" rather than "401 Unauthorized".

## 7. Acceptance Criteria

- **AC-301**: Given a valid token is received, when validation completes successfully, then the player lands on the game entry screen and the token is written to localStorage (`access_token`).
- **AC-302**: Given a token is received, when the backend responds that the token is expired, then the system displays the `auth.tokenExpired` screen (localized in `ja`) and does not show any game content.
- **AC-303**: Given a token is received, when the backend returns 401, then the system displays the `auth.unauthorized` screen and does not show any game content.
- **AC-304**: Given a token is received, when the backend returns 500 or 503, then the system displays the `auth.serverError` screen with a visible retry button; tapping retry re-submits the same token for validation.
- **AC-305**: Given no URL param, no localStorage token, and no JS bridge message within the wait period, when the wait period elapses, then the system displays the `auth.tokenMissing` screen.
- **AC-306**: Given a player is on the `auth.serverError` screen, when they tap retry and validation now succeeds, then the player is navigated to the game entry screen.
- **AC-307**: Given a player is on any auth error screen, when they inspect the page, then no raw token value is visible in the UI or accessible via browser developer tools.
- **AC-308**: Given a valid token is stored in localStorage, when the WebView is opened on a subsequent page without a URL param, then the stored token is re-validated and the player is granted access without requiring a new mobile app injection.
- **AC-309**: Given a token sourced from localStorage fails validation (expired or invalid), when the error screen is shown, then `localStorage.access_token` has been removed.
- **AC-310**: Given `VITE_ENV=local`, when the WebView loads without any URL param or localStorage token, then the game loads immediately without showing any auth error screen — no URL token, no bridge message, no 5-second wait required.
- **AC-311**: Given `VITE_ENV=local`, after successful auth, then `localStorage.access_token` is absent — `local-dev-token` is never written to storage.

## 8. Test & Validation Criteria

**Test Perspectives:**

- **Happy path (URL param):** Valid token via `?accessToken=` → loading screen → game entry screen → token saved to localStorage.
- **Happy path (JS bridge):** Valid token via bridge message → loading screen → game entry screen → token saved to localStorage.
- **Happy path (localStorage):** No URL param; valid token in localStorage → loading screen → game entry screen (no mobile app re-injection needed).
- **Happy path (URL param overwrites storage):** Stale token in localStorage; fresh token via `?accessToken=` → fresh token validated and overwrites localStorage.
- **Token expired (localStorage source):** Stored token is expired → backend returns expired error → token cleared from localStorage → `auth.tokenExpired` screen.
- **Token expired (URL/bridge source):** Backend returns expired error → `auth.tokenExpired` screen shown, no game content visible.
- **Token invalid signature:** Backend returns invalid error → `auth.tokenInvalid` screen shown.
- **Unauthorized:** Backend returns 401 → `auth.unauthorized` screen shown.
- **Server error with retry:** Backend returns 500 → `auth.serverError` screen with retry button; retry succeeds → game loads.
- **Token missing:** Page loads, no URL param, no localStorage token, no bridge message within wait period → `auth.tokenMissing` screen.
- **Empty param:** `?accessToken=` in URL, no bridge message, no localStorage token → treated as missing.
- **Security — no bypass:** Directly navigating to the game URL without a token or stored session → redirected to auth gate, not the game.
- **Locale:** All error screens render in `ja` by default; switch to `en` locale → all strings update correctly.
- **Retry on server error:** 503 → retry → 200 → game entry screen.
- **Local dev (`VITE_ENV=local`):** App loads with no URL token and no bridge → game loads immediately using `local-dev-token`; no `auth.tokenMissing` screen, no 5s wait. Backend (`ENV=local`) must return valid response for `local-dev-token`.
- **Local dev — game-api unavailable:** `VITE_ENV=local` but game-api not running → validation call fails → `auth.serverError` screen shown with retry (same error handling as production).

## 9. Out of Scope

- Token refresh or silent re-authentication mid-session.
- Differentiating between a token that is structurally malformed (bad JWT format) versus cryptographically invalid — both map to `auth.tokenInvalid` from the frontend's perspective.
- Showing game content while validation is pending (optimistic rendering).
- Any analytics or logging of auth failure rates — that is a backend/observability concern.
- Deep-linking to a specific game state via the token or URL params other than `token`.
